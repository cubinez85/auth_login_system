# Flask configuration
FLASK_APP=run.py
FLASK_ENV=production
FLASK_DEBUG=0
SECRET_KEY=******
JWT_SECRET_KEY=******

# Database configuration
DATABASE_URL=postgresql://cubinez85:******@localhost:5432/auth_system_db

# JWT configuration
JWT_ACCESS_TOKEN_EXPIRES=900  
JWT_REFRESH_TOKEN_EXPIRES=604800  

# Admin configuration
ADMIN_EMAIL=cubinez85@cubinez.ru
USERNAME=admin
ADMIN_PASSWORD=*******

RATELIMIT_STORAGE_URL=redis://localhost:6379/0

# === Password Reset Settings ===
PASSWORD_RESET_EXPIRES_HOURS=1
PASSWORD_RESET_MIN_INTERVAL_MINUTES=15

# === Email Settings (ваш почтовый сервер) ===
MAIL_SERVER=95.174.94.246
MAIL_PORT=25
MAIL_USE_TLS=false
MAIL_USE_SSL=false

# Отправитель
MAIL_DEFAULT_SENDER=cubinez85@cubinez.ru

# Email verification
EMAIL_VERIFICATION_EXPIRES_HOURS=24
EMAIL_VERIFICATION_MIN_INTERVAL_MINUTES=5

# Base URL
#BASE_URL=https://auth.cubinez.ru

BASE_URL=http://localhost:8084

# Логирование вместо отправки (для отладки)
MAIL_LOG_ONLY=false
